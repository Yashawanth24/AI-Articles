<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="<%= article.summary.slice(0, 150) %>">
  <title><%= article.title %></title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <header>
    <h1>Tech News Hub</h1>
    <nav>
      <a href="/">Home</a>
      <a href="/category/smartphones">Smartphones</a>
      <a href="/category/wearables">Wearables</a>
    </nav>
  </header>
 <main class="article-container">
    <nav class="breadcrumb">
        <a href="/">Home</a> / <span><%= article.category %></span>
    </nav>

    <header class="article-header">
        <h1><%= article.title %></h1>
        <p class="meta">Published on <%= new Date(article.publishedAt).toDateString() %> | AI Analysis</p>
    </header>

    <% if (article.image) { %>
        <img src="<%= article.image %>" class="featured-image">
    <% } %>

    <div class="article-body">
        <%- article.content.replace(/\n/g, '<br>') %>
    </div>

    <footer class="article-footer">
        <p>This article was synthesized by AI based on reporting from: 
           <a href="<%= article.url %>" target="_blank"><%= article.source || 'Original Source' %></a>
        </p>
    </footer>
</main>
</body>
</html>